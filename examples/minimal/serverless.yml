service: minimal-example

plugins:
  - ./node_modules/serverless-esbuild
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x

functions:
  validateIsin:
    handler: index.handler
    events:
      - http:
          path: validate-isin/{isin}
          method: get
